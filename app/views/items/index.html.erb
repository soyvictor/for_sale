<p style="margin:8px;">Hey, I'm Victor.  These are currently all the things I have for sale in Mexico City, specifically in Azcapotzalco.  I may be able to deliver some items.  You can contact me on Whatsapp at xxxxxxxxxxxxxxx.</p>

<div class="container mt-3">
  <div class="row justify-content-center">
    <div class="column">
      <% @items.each do |item| %>
        <%= link_to item_path(item) do%>
          <div class="item-box" style="position: relative;">
            <% if item.photos.attached? %>
          <!--     <div style="max-width: 400px;">
                 <div id="carouselExampleControls" class="carousel slide carousel-slider" data-ride="carousel" data-interval="false">

                  <div class="carousel-inner rounded mb-3" style="width: 100%; height: 100% !important;">
                    <div class="carousel-item active">
                      <%= cl_image_tag item.photos.first.key, width: 325, height: 240, crop: :pad, class: "d-block w-100 h-100 carousel-img", alt: "first slide" %>
                    </div>


                    <% item.photos.drop(1).each do |photo| %>

                      <div class="carousel-item">
                        <%= cl_image_tag photo.key, width: 325, height: 240, crop: :pad,  class: "d-block w-100 h-100 carousel-img", alt: "a slide" %>
                      </div>
                    <% end %>
                  </div>
                  <% if item.photos.length > 1 %>
                  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                  </a>
                  <% end %>
                  <% if item.photos.length > 1 %>
                  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                  </a>
                  <% end %>
                </div>
              </div> -->
              <%= cl_image_tag item.photos.first.key, width: 325, height: 240, crop: :pad,  class: "img-rounded", alt: "first slide" %>
            <% else %>
              <img src="https://picsum.photos/325/240" class="img-rounded" alt="">
            <% end %>
            <% if item.photos.count > 1 %>
              <div class="display-image-count">image 1 of <%= item.photos.count %></div>
            <% end %>
            <p class="item-name"><span class="item-name-underline"><%= item.name %></span><span class="price">$<%= item.price %></span></p>
            <p class="price corner-price">$<%= item.price %></p>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>


<%= javascript_pack_tag 'items' %>
<%= javascript_pack_tag 'index' %>
<%= javascript_pack_tag 'carousel' %>

